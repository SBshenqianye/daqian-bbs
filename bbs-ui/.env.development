# 开发环境配置
ENV = 'development'
# 本地接口地址代理标识
VUE_APP_BBS_API = '/user-api'
# 本地静态文件访问及接口地址
VUE_APP_BBS_BASE_FILE = '/bbs-server'
# 后端服务器地址（仅 vue.config.js proxy 使用，非 VUE_APP_ 前缀不会暴露给前端代码）
DEV_BACKEND_URL = 'http://127.0.0.1:9083'

